{% extends 'base.html.twig' %}

{% block title %}Log index{% endblock %}

{% block body %}
    <h1>Log index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Author</th>
                <th>CreatedAt</th>
                <th>Description</th>
                <th>ActionType</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for log in logs %}
            <tr>
                <td>{{ log.id }}</td>
                <td>{{ log.author }}</td>
                <td>{{ log.createdAt ? log.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ log.description }}</td>
                <td>{{ log.actionType }}</td>
                <td>
                    <a href="{{ path('app_log_show', {'id': log.id}) }}">Détails</a>
                    {# <a href="{{ path('app_log_edit', {'id': log.id}) }}">edit</a> #}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {# <a href="{{ path('app_log_new') }}">Create new</a> #}
{% endblock %}
